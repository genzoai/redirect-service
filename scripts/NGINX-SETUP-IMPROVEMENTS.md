# Nginx Setup Script - Safety Improvements v2.0.0

## Что улучшено

### ✅ 1. Автоматический Backup

**До:**
- Никаких backup не создавалось
- При ошибке невозможно откатиться

**После:**
```bash
Step 0: Creating Backup
✓ Backed up nginx.conf
✓ Backed up sites-available/
✓ Backed up sites-enabled/
✓ Backed up conf.d/
✓ Backup created: /etc/nginx/backup-20260113-143022
```

**Что делается:**
- Автоматически создается backup всей конфигурации nginx
- Backup хранится в `/etc/nginx/backup-YYYYMMDD-HHMMSS/`
- Можно пропустить с флагом `--skip-backup`

---

### ✅ 2. Проверка Существующей Конфигурации

**До:**
- Файл конфигурации перезаписывался без предупреждения
- Можно было потерять настройки

**После:**
```
Checking for Existing Configuration
⚠ Configuration file already exists: /etc/nginx/sites-available/go.example.com
⚠ Site is already enabled: /etc/nginx/sites-enabled/go.example.com

WARNING: An nginx configuration for go.example.com already exists.

Options:
  1) Overwrite existing configuration (backup will be created)
  2) Keep existing configuration and exit
  3) Show existing configuration

Choose option [1-3]:
```

**Что делается:**
- Проверяет наличие конфигурации для указанного домена
- Даёт возможность:
  - Перезаписать (с backup)
  - Оставить как есть
  - Посмотреть текущую конфигурацию

---

### ✅ 3. Интерактивные Запросы

**До:**
- Nginx устанавливался автоматически без вопросов
- Default site удалялся автоматически

**После:**

**Установка Nginx:**
```
⚠ Nginx is not installed

Install nginx now? [Y/n]:
```

**Удаление Default Site:**
```
Default nginx site is currently enabled.
Disable default site? [Y/n]:
```

**Что делается:**
- Спрашивает разрешение перед установкой nginx
- Спрашивает перед удалением default site
- Можно оставить default site работающим

---

### ✅ 4. Автоматический Rollback

**До:**
- При ошибке конфигурация оставалась сломанной
- Нужно было вручную восстанавливать

**После:**
```
✗ Setup failed with exit code 1

Rollback to previous configuration? [Y/n]: Y

⚠ Rolling back nginx configuration...
✓ Restored nginx.conf
✓ Restored sites-available/
✓ Restored sites-enabled/
✓ Restored conf.d/
✓ Rollback complete, nginx reloaded
```

**Что делается:**
- При любой ошибке предлагает откатить изменения
- Автоматически восстанавливает из backup
- Проверяет конфигурацию и перезагружает nginx

---

### ✅ 5. Управление Backup Директориями

**После установки:**
```
ℹ Backup directory: /etc/nginx/backup-20260113-143022
To remove backup: sudo rm -rf /etc/nginx/backup-20260113-143022

⚠ Found 7 backup directories. Consider cleaning old backups.
List backups: ls -lad /etc/nginx/backup-*
```

**Что делается:**
- Показывает путь к созданному backup
- Предупреждает если накопилось > 5 backup
- Даёт команду для просмотра и очистки

---

### ✅ 6. Поддержка Флагов

**Новые опции:**

```bash
# Стандартный запуск (с backup)
sudo bash scripts/setup-nginx.sh go.example.com

# Пропустить backup
sudo bash scripts/setup-nginx.sh --skip-backup go.example.com

# Справка
sudo bash scripts/setup-nginx.sh --help
```

**Вывод --help:**
```
Usage: sudo bash setup-nginx.sh [OPTIONS] [DOMAIN]

Options:
  --skip-backup    Skip automatic backup of nginx configuration
  --help           Show this help message

Examples:
  sudo bash scripts/setup-nginx.sh go.example.com
  sudo bash scripts/setup-nginx.sh --skip-backup go.example.com
```

---

## Сценарии Использования

### 🟢 Первая Установка (новый сервер)

```bash
sudo bash scripts/setup-nginx.sh go.example.com
```

**Что произойдёт:**
1. ✅ Создаст backup (будет пустой)
2. ✅ Не найдёт существующей конфигурации
3. ✅ Спросит разрешение установить nginx (если не установлен)
4. ✅ Создаст конфигурацию
5. ✅ Не будет удалять default site (если вы откажетесь)

---

### 🟡 Переустановка (тот же домен)

```bash
sudo bash scripts/setup-nginx.sh go.example.com
```

**Что произойдёт:**
1. ✅ Создаст backup существующей конфигурации
2. ⚠️ Обнаружит существующую конфигурацию для go.example.com
3. ❓ Спросит: перезаписать / оставить / показать
4. Если выберете "перезаписать":
   - ✅ Создаст новую конфигурацию
   - ✅ Можно откатиться к backup

---

### 🔴 Установка на Production (уже есть другие сайты)

```bash
sudo bash scripts/setup-nginx.sh redirect.mysite.com
```

**Что произойдёт:**
1. ✅ Создаст backup ВСЕЙ конфигурации nginx
2. ✅ Не тронет конфигурации других доменов
3. ✅ Создаст конфигурацию только для redirect.mysite.com
4. ❓ Спросит про default site (можно оставить)
5. ✅ Ваши другие сайты продолжат работать

**Безопасность:**
- Backup сохранён в `/etc/nginx/backup-YYYYMMDD-HHMMSS/`
- При ошибке можно откатиться
- Другие домены не затронуты

---

## Восстановление из Backup

### Автоматический Rollback (при ошибке)

Если что-то пошло не так, скрипт сам предложит:
```
Rollback to previous configuration? [Y/n]:
```

### Ручное Восстановление

Если нужно откатиться позже:

```bash
# 1. Посмотреть доступные backup
ls -lad /etc/nginx/backup-*

# 2. Восстановить из конкретного backup
sudo cp -r /etc/nginx/backup-20260113-143022/* /etc/nginx/

# 3. Проверить конфигурацию
sudo nginx -t

# 4. Перезагрузить nginx
sudo systemctl reload nginx
```

---

## Удаление Старых Backup

```bash
# Посмотреть все backup
ls -lad /etc/nginx/backup-*

# Удалить конкретный backup
sudo rm -rf /etc/nginx/backup-20260113-143022

# Удалить все backup старше 30 дней
sudo find /etc/nginx -maxdepth 1 -name "backup-*" -type d -mtime +30 -exec rm -rf {} \;

# Оставить только последние 5 backup
cd /etc/nginx && sudo ls -t -d backup-* | tail -n +6 | xargs rm -rf
```

---

## Безопасность

### ✅ Что НЕ сломается:

1. **Существующие сайты** - конфигурации других доменов не трогаются
2. **Текущие соединения** - nginx reload без разрыва соединений
3. **Default site** - можно оставить работающим
4. **Главная конфигурация** - nginx.conf модифицируется минимально (только добавление include)

### ⚠️ Что может измениться:

1. **Default site** - будет предложено отключить (можно отказаться)
2. **Конфигурация для того же домена** - будет предложено перезаписать (можно отказаться)
3. **nginx.conf на RHEL/CentOS** - добавится `include /etc/nginx/sites-enabled/*` (безопасно)

### 🛡️ Защита от ошибок:

1. **Всегда создаётся backup** перед изменениями
2. **Проверка конфигурации** перед reload
3. **Автоматический rollback** при ошибке
4. **Интерактивные запросы** для критичных операций

---

## Миграция с Версии 1.x

Если вы уже установили с помощью старого скрипта:

```bash
# Просто запустите новый скрипт
sudo bash scripts/setup-nginx.sh go.example.com

# Он обнаружит существующую конфигурацию и предложит:
# 1) Перезаписать (безопасно, создаст backup)
# 2) Оставить как есть
# 3) Посмотреть текущую конфигурацию
```

**Рекомендуем выбрать опцию 3**, чтобы убедиться, что текущая конфигурация вас устраивает, затем выбрать 2 (оставить как есть).

---

## Вопросы и Ответы

**Q: Обязательно ли создавать backup каждый раз?**

A: Нет, можно пропустить с флагом `--skip-backup`, но не рекомендуется на production.

**Q: Сколько места занимает backup?**

A: Обычно 10-100 KB (только конфигурационные файлы, не логи).

**Q: Как часто чистить старые backup?**

A: Рекомендуем оставлять последние 5. Скрипт предупредит если их больше 5.

**Q: Что делать если rollback не сработал?**

A: Восстановите вручную:
```bash
sudo cp -r /etc/nginx/backup-YYYYMMDD-HHMMSS/* /etc/nginx/
sudo nginx -t
sudo systemctl reload nginx
```

**Q: Можно ли запустить скрипт без интерактивных запросов?**

A: Сейчас нет, но можно добавить флаг `--force` в будущих версиях.

---

## Changelog

### Version 2.0.0 (2026-01-13)

**Added:**
- ✨ Automatic backup before any changes
- ✨ Detection of existing configurations
- ✨ Interactive prompts for destructive operations
- ✨ Automatic rollback on errors
- ✨ `--skip-backup` flag
- ✨ `--help` flag
- ✨ Backup management warnings

**Changed:**
- 🔄 Nginx installation now asks for confirmation
- 🔄 Default site removal now asks for confirmation
- 🔄 Domain can be passed as argument or prompted

**Fixed:**
- 🐛 No longer overwrites existing configs without warning
- 🐛 No longer removes default site without permission
- 🐛 Better error handling and recovery

---

## Контакты

Если нашли баг или есть предложения:
- Откройте issue в GitHub репозитории
- Или отредактируйте этот скрипт и отправьте PR

**Genzo AI LLP © 2026**
